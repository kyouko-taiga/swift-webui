
const initialState = {
    id: '12345',
    name: 'swift-webui',
    currentBranch: 'master'
}


const repository = (state = initialState, action) => {
    switch (action.type) {
    default:
        return state
    }
}

export default repository
